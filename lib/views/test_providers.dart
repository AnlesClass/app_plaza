import 'package:flutter/material.dart';

class TestProviders extends StatelessWidget {
  const TestProviders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
